<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Katalog Produk</title>
    <!-- Custom CSS -->
    <style>
        body {
            font-family: Arial, sans-serif;
            padding-top: 20px;
            max-width: 1000px;
            margin: 0 auto;
        }
        .container {
            display: flex;
            /* flex-wrap: wrap; */
            justify-content: center;
            gap: 20px;
            max-width: 100%; 
        }
        .timnas-card {
            min-width: 210px;
            min-width: 210px;
            border: 1px solid #ddd;
            border-radius: 5px;
            background-color: rgb(255, 255, 255); 
            color: #000000;
            overflow: hidden;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
            display: flex;
            flex-direction: column;
            height: auto;
        }
        .timnas-card:hover {
            transform: translateY(-7px);
            opacity: 90%;
        }
        .timnas-image {
            object-fit: cover;
            width: 100%;
            height: 200px;
        }
        .timnas-info {
            margin-top: -30px;
            padding: 10px;
            flex-grow: 1;
        }
        .timnas-title {
            margin-bottom: -10px;
            font-size: 16px;
            line-height: 1.2;
            overflow: hidden;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            text-overflow: ellipsis;
        }
        .timnas-no {
            margin-bottom: 5px;
            font-size: 14px;
            color: #000000;
        }
        .timnas-rating {
            text-align: center;
        }
        .rating-text {
            display: inline-block;
            margin-right: 5px;
        }

        .star-rating {
            display: inline-block;
        }

        .material-icons {
            vertical-align: middle;
        }
        
        .sticky-back {
            position: sticky;
            top: 0;
            z-index: 1000;
        }
    </style>
</head>
<body>
    <div ">

            <img src="https://shorturl.at/kmryA" alt="GARUDA" style="width: 50px; text-align: left;">
            <a href="/timnas"><img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTD7NXZEn3fBrdWBKhseTHb2cznzjAQ1R8akfaF1FUf1Q&s" alt="" width="50px" style="transform: scaleX(-1); margin-right: 10px;"></a>
    </div>
    <h2 style="text-align: center;">Daftar Nama Pemain Timnas U-21</h2>
    <div class="container">
        <% timnas.forEach((timnas, index) => { %>
            <div class="timnas-card" onclick="window.location.href='/timnas/detail_pemain/<%= timnas._id %>';">
                            <img src="<%= timnas.foto %>" class="timnas-image" alt="<%= timnas.nama %>">
                <div class="timnas-info">
                    <h5 class="timnas-title"><%= timnas.nama %></h5>
                    <p class="timnas-no">Nomor Punggung: <%= timnas.no %></p>
                </div>
            </div>
        <% }) %>
    </div>
</body>
</html>
